# Deskline logo gallery

Browse live: http://127.0.0.1:8787/logos

All marks in `web/static/img/logos/*.svg` are hand-tuned vectors (brand green `#1f6b56`).

Current default app mark (until you pick): **08 Classic Solid** → `web/static/img/logo.svg`

Reply with favorites, e.g. `08 + 12` or `03 icon, 12 lockup`.
